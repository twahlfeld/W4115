int add(x, y) {
  return x + y;
}

int sub(x, y) {
  return x - y;
}

int mult(x, y) {
  return x * y;
}

int div(x, y) {
  return x / y;
}

int main() {

  add(1, 2);
  sub(2, 1);
  mult(2, 3);
  div(4, 2);

  print(stdout, "Hello World\n");

}


