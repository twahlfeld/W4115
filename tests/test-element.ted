int main() {
  Page p;
  Element e;
  List l;
  List l1;
  Element f;
  string s;
  string t;

  p = fetch("http://haighr.com");
  e = pageroot(p);

  l = elementchildren(e);
  l1 = listtail(l);
  f = listhead(l1);

  s = elementattr(f, "id");
  t = elementtype(f);

  print(stdout, "%s\n%s\n", s, t);

  return 0;
}
