int main() {
  Page p;
  Element e;
  List l;
  Element f;
  string s;
  string t;

  p = fetch("http://haighr.com");
  e = pageroot(p);

  l = elementchildren(e);
  f = listnext(l);

  s = elementattr(f, "id");
  t = elementtype(f);

  print(stdout, "%s\n%s\n", s, t);

  return 0;
}
