int main() {

  Page p = fetch("http://haighr.com");
  Elememt e = pageroot(p);

  List l = elementchildren(e);
  Element f = listnext(l);

  string s = elementattr(f, "id");
  string t = elementtype(f);

  print(stdout, "%s\n%s\n", s, t);

  return 0;
}
