int main() {

  Page p;
  string url;
  string html;

  p = fetch("https://haighr.com");

  url = pageurl(p);
  print(stdout, "%s\n", url);

  html = pagehtml(p);  
  print(stdout, "%s\n", html);

  return 0;
}

